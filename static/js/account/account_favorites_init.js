/**
 * Favorites page initialization
 *
 * Initializes the favorites page with search and management functionality.
 */

import { initFavoritesPage } from './account_favorites.js';

// Initialize favorites page when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    initFavoritesPage();
});
